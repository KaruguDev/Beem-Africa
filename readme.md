## Installation

1. change directory 
    - `cd /PATH/<project folder>`

2. install node modules
    - `npm install`

## Run

1. install nodemon

2. run project locally
    - `nodemon <project folder>`